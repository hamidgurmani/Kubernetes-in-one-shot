apiVersion: v1
kind: Service
metadata:
  name: {{ include "notes-js-app.fullname" . }}
  labels:
    {{- include "notes-js-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "notes-js-app.selectorLabels" . | nindent 4 }}
