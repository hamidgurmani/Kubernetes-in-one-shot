apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notes-js-app.fullname" . }}-test-connection"
  labels:
    {{- include "notes-js-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notes-js-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
